import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'minty-ai-art',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
